#
# Manifeste du module « CustomModule2 »
#
# Généré par : david
#
# Généré le : 23/09/2026
#

@{

# Module de script ou fichier de module binaire associé à ce manifeste.
RootModule = 'CustomModule2.psm1'

# Numéro de version de ce module.
ModuleVersion = '1.0.0'

# Éditions PowerShell prises en charge
# CompatiblePSEditions = @()

# ID utilisé pour identifier ce module de manière unique
GUID = '7c65bbc5-97eb-4e1c-abc0-5c7af7b2ad8c'

# Auteur de ce module
Author = 'david'

# Société ou fournisseur de ce module
CompanyName = 'Inconnu'

# Déclaration de copyright pour ce module
Copyright = '(c) david. Tous droits réservés.'

# Description de la fonctionnalité fournie par ce module
# Description = ''

# Version minimale du moteur PowerShell requise par ce module
# PowerShellVersion = ''

# Nom de l’hôte PowerShell requis par ce module
# PowerShellHostName = ''

# Version minimale de l’hôte PowerShell requis par ce module
# PowerShellHostVersion = ''

# Version minimale de Microsoft .NET Framework requise par ce module. Cette condition préalable est valide uniquement pour l’édition PowerShell Desktop.
# DotNetFrameworkVersion = ''

# Version minimale du CLR (Common Language Runtime) requise par ce module. Cette condition préalable est valide uniquement pour l’édition PowerShell Desktop.
# ClrVersion = ''

# Architecture du processeur (None, X86, Amd64) requise par ce module
# ProcessorArchitecture = ''

# Modules qui doivent être importés dans l’environnement global avant l’importation de ce module
# RequiredModules = @()

# Assemblys qui doivent être chargés avant l’importation de ce module
# RequiredAssemblies = @()

# Fichiers de script (.ps1) exécutés dans l’environnement de l’appelant avant l’importation de ce module.
# ScriptsToProcess = @()

# Fichiers de type (.ps1xml) à charger lors de l’importation de ce module
# TypesToProcess = @()

# Fichiers de format (.ps1xml) à charger lors de l’importation de ce module
# FormatsToProcess = @()

# Modules à importer en tant que modules imbriqués du module spécifié dans RootModule/ModuleToProcess
# NestedModules = @()

# Fonctions à exporter à partir de ce module. Pour de meilleures performances, n’utilisez pas de caractères génériques et ne supprimez pas l’entrée. Utilisez un tableau vide s’il n’y a aucune fonction à exporter.
FunctionsToExport = '*'

# Cmdlets à exporter à partir de ce module. Pour de meilleures performances, n’utilisez pas de caractères génériques et ne supprimez pas l’entrée. Utilisez un tableau vide s’il n’y a aucune cmdlet à exporter.
CmdletsToExport = '*'

# Variables à exporter depuis ce module
VariablesToExport = '*'

# Les alias à exporter à partir de ce module. Pour de meilleures performances, n’utilisez pas de caractères génériques et ne supprimez pas l’entrée. Utilisez un tableau vide s’il n’y a aucun alias à exporter.
AliasesToExport = '*'

# Ressources DSC à exporter depuis ce module
# DscResourcesToExport = @()

# Liste de tous les modules empaquetés avec ce module
# ModuleList = @()

# Liste de tous les fichiers empaquetés avec ce module
# FileList = @()

# Données privées à transmettre au module spécifié dans RootModule/ModuleToProcess. Cela peut également contenir une table de hachage PSData avec des métadonnées de module supplémentaires utilisées par PowerShell.
PrivateData = @{

    PSData = @{

        # Balises appliquées à ce module. Elles aident à la découverte de modules dans les galeries en ligne.
        # Tags = @()

        # URL de la licence de ce module.
        # LicenseUri = ''

        # URL du site web principal de ce projet.
        # ProjectUri = ''

        # URL d’une icône représentant ce module.
        # IconUri = ''

        # Notes de publication de ce module
        # ReleaseNotes = ''

        # Chaîne de préversion de ce module
        # Prerelease = ''

        # Indique si le module nécessite une acceptation explicite de l’utilisateur pour l’installation, la mise à jour ou l’enregistrement
        # RequireLicenseAcceptance = $false

        # Modules dépendants externes de ce module
        # ExternalModuleDependencies = @()

    } # Fin de la table de hachage PSData

} # Fin de la table de hachage PrivateData

# URI HelpInfo de ce module
# HelpInfoURI = ''

# Préfixe par défaut des commandes exportées à partir de ce module. Remplacez le préfixe par défaut à l’aide de Import-Module -Prefix.
# DefaultCommandPrefix = ''

}

